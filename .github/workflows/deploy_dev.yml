name: Deploy Dev

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter environment
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.0'
          channel: 'stable'

      - name: Get Flutter dependencies
        run: flutter pub get

      - name: Inject .env files
        run: |
          echo "API_ENDPOINT=${{ secrets.API_ENDPOINT }}" >> .env.dev
          echo "CLIENT_ID=${{ secrets.CLIENT_ID }}" >> .env.dev
          echo "CLIENT_SECRET=${{ secrets.CLIENT_SECRET }}" >> .env.dev
          echo "" >> .env.staging
          echo "" >> .env.prod

      - name: Generate Flutter files
        run: flutter packages pub run build_runner build --delete-conflicting-outputs

      - name: Build APK
        run: flutter build apk --flavor dev --release --build-number $GITHUB_RUN_NUMBER

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dev-release-apk
          path: build/app/outputs/flutter-apk/app-dev-release.apk

      - name: Upload Android APK to Firebase
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID_DEV }}
          token: ${{ secrets.FIREBASE_TOKEN }}
          groups: tester
          releaseNotes: Dev build
          file: build/app/outputs/flutter-apk/app-dev-release.apk
